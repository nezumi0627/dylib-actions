name: Build & Release Swift dylib

on:
  push:
    branches: [ '**' ]
    tags:     [ 'v*' ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build macOS dylib (Release)
        run: swift build -c release

      - name: Build iOS arm64 dylib (Release)
        run: |
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)
          swift build -c release --triple arm64-apple-ios15.0 --sdk "$SDK_PATH" --build-path .build/ios
          mkdir -p .build/release-ios
          cp .build/ios/arm64-apple-ios15.0/release/libMyLibrary.dylib .build/release-ios/libMyLibrary_iOS.dylib

      - name: Upload CI artifacts
        uses: actions/upload-artifact@v4
        with:
          name: MyLibrary.dylibs
          path: |
            .build/release/libMyLibrary.dylib
            .build/release-ios/libMyLibrary_iOS.dylib

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download dylib artifact
        uses: actions/download-artifact@v4
        with:
          name: MyLibrary.dylib
          path: dist

      - name: Zip dylib
        run: |
          cd dist
          zip MyLibrary-${{ github.ref_name }}.zip libMyLibrary.dylib

      - name: Create GitHub Release & upload asset
        uses: softprops/action-gh-release@v2
        with:
          files: dist/MyLibrary-${{ github.ref_name }}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
